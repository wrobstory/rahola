# Rahola Lab

`rahola-lab` contains the frozen reference campaigns, shared evaluation referee,
forecast baselines, and conformal alarm experiments layered on the validated
`rahola` dynamics package. The core physics package remains a separate workspace
member and is not modified by laboratory code.

The architecture and complete regeneration commands are filled in alongside the
reference campaign and experiment milestones.

